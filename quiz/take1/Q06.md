# Q06 — Opt-in pipe + which ValidationPipe (INCOMPLETE — take1)

> Status: **not locked.** Candidate answered the core correctly but the final pull (failure chain +
> which AC fails) was never closed before the session was restarted. Banked as partial.

## Question (as asked)

You add `@IsNotEmpty()` to `CreateArticleDto`. An empty body is still accepted. What single thing is
missing from the controller, and where exactly does it go?

## Candidate's answer

`@UsePipes(new ValidationPipe())`, on **both** actions that depend on `CreateArticleDto` — the
`create`/`update` actions of `ArticleController` (`backend/src/article/article.controller.ts`).

## Coach pushback + progress

**Pushback (completeness) — which `ValidationPipe`, and what breaks with the wrong one?**
- Candidate: the app's own pipe, `backend/src/shared/pipes/validation.pipe.ts`. ✅ (correct file —
  not `@nestjs/common`'s)
- Candidate identified that the own pipe throws its own errors / status / messages
  (`BadRequestException('No data submitted')`; `HttpException({ message, errors: buildError() }, 400)`).

**Still open at restart:** the candidate did not state the one-line **failure chain** for importing
the default pipe, nor name the **acceptance criterion** that fails.

Correct close (for take2 reference — do NOT hand it to the candidate; make them produce it):
- Default `@nestjs/common` pipe returns `{ statusCode, message: string[], error }` — **no `errors`
  key**. The frontend decodes with `object({ errors: genericErrorsDecoder })`
  (`frontend/src/services/conduit.ts:41,64,74,84,99`), which **requires** `errors` → decoder rejects
  the payload → the error never reaches the editor's `.match({ err })` branch → field error never
  renders → **AC3 fails** (error must surface in the editor UI; silent failure).

## Coaching-system work done during this question (carried forward, not take-specific)

- `01-COACH.md` pipe section rewritten SHOW-DON'T-TELL: real pipe source incl. `buildError`, plus
  captured `// own pipe` vs `// built-in` responses for empty-body and validation-failure.
- Removed the fabricated `SlugValidationPipe`/`UpdateArticleDto` snippet; grounded in the real
  `article.update` handler.
- `01-COACH.md` EDIT POLICY: added SHOW-DON'T-TELL + SHOW-BOTH-SIDES-TOGETHER rules.
- `02-VOICE.md`: re-anchor-the-spec rule, no-riddle rule, no-riddle⇄no-ghostwrite balance gate,
  MECE repetition-angle rule, clickable `path:line` "point with precision" rule.
- `03-TUTORIAL.md`: Q1/Q2 now quote the user story; Q5 reformulated to a pure review question
  (registration moved out as Q4's territory, MECE).
