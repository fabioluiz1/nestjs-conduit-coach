# Q06 — Opt-in ValidationPipe (controller wiring)

## Question (as asked)

You add `@IsNotEmpty()` to `CreateArticleDto`. An empty body is still accepted. What single thing is
missing from the controller — and where exactly does it go (which decorator, on what, applied where)?

## Candidate's answer

`@UsePipes(new ValidationPipe())`, from the app's `shared/pipes`, applied to the controller route
methods that depend on `CreateArticleDto`: create and update.

## Coach pushback 1 (supplementary — status code, posed as a pull)

3-star on the core. Supplementary `plan.md` angle: when that pipe rejects a bad body, what HTTP
status does it throw today, what's the RealWorld convention for a validation failure, and what's the
blast radius of changing it?

## Candidate's response to pushback 1

It throws `BAD_REQUEST` (400); the RealWorld convention would use `UNPROCESSABLE_ENTITY` (422) for
validation errors.

## Coach pushback 2 (blast radius)

You'd be editing the `HttpStatus` inside the **shared** `shared/pipes/validation.pipe.ts`. Who else
uses that exact pipe, and therefore what else starts returning 422 the moment you flip it? Name the
affected endpoints.

## Candidate's response to pushback 2

The blast radius is changing the shared `ValidationPipe`, because all the routes that use validation
reuse that module's logic.

## Coach pushback 3 (name the handlers) → candidate reframes

Coach: name exactly which handlers carry `@UsePipes` today (file + route); if unsure, `01-COACH.md:499-502`.

Candidate: I'd rather ask Cline to grep and list them. This question isn't relevant to my rubric —
it's context an agent can gather and include in the plan.

## Coach resolution (concession + close)

Conceded — enumerating endpoints from memory is **recall**, not graded. Delegating the grep to Cline
is correct and is *good* AI Usage (gather: "grep `@UsePipes`/`ValidationPipe` usages, list handlers +
routes, change nothing"). Two things kept straight: (1) the blast-radius **awareness** IS graded
(Plan Soundness) and the candidate already has it (knew the pipe is shared); the grep **sizes** the
radius, it doesn't discover it. (2) The decision stays the candidate's: once Cline reports
`user.controller.ts` `create` + `login`, consciously accept "422 there too is the RealWorld
convention → a consistency win, not a regression" and record THAT in `plan.md`. Cline gathers; the
candidate judges; the plan records the judgment.

## Grade

**3-star** — full judgment cluster (opt-in `@UsePipes` on both handlers, repo's own pipe, 400→422
plan decision, shared-pipe blast radius) **plus** a 3-star AI-Usage instinct: delegate recall to
Cline, own the decision.
